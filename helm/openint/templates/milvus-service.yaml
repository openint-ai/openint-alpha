apiVersion: v1
kind: Service
metadata:
  name: openint-milvus
  labels:
    app: openint-milvus
    {{- include "openint.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.milvus.port }}
      targetPort: {{ .Values.milvus.port }}
      protocol: TCP
  selector:
    app: openint-milvus
