# OpenInt Backend Requirements

# Web framework
flask>=3.0.0
flask-cors>=4.0.0

# API (optional - can use FastAPI instead)
# fastapi>=0.104.0
# uvicorn>=0.24.0

# Environment
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0
redis>=5.0.0
numpy>=1.24.0

# modelmgmt-agent (semantic analysis): required so backend can import modelmgmt_agent.semantic_analyzer
sentence-transformers>=2.2.0

# openint-vectordb / search agent (Milvus): required so search_agent can import milvus_client
pymilvus>=2.3.0

# Observability: OpenTelemetry tracing + structured JSON logging
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
opentelemetry-instrumentation-flask>=0.49b0
opentelemetry-instrumentation-logging>=0.49b0
