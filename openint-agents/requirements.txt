# Bank Agents System Requirements

# Core dependencies
pymilvus>=2.3.0
sentence-transformers>=2.2.0
python-dotenv>=1.0.0

# Agent framework (optional - can use LangChain, AutoGen, etc.)
# langchain>=0.1.0
# autogen>=0.2.0

# Message bus and agent state (Redis for persistent state)
redis>=5.0.0
# pika>=1.3.0  # RabbitMQ

# API framework (if exposing REST API)
# fastapi>=0.104.0
# uvicorn>=0.24.0

# Utilities
pydantic>=2.0.0

# Orchestration (LangGraph)
langgraph>=0.2.0

# sg-agent sentence generation uses Ollama (open-source LLM); no extra Python deps (urllib)
